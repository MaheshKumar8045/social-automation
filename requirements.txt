# Runtime dependency set for the main Windows .venv.
# Note: aisignal is intentionally NOT listed; aisignal==1.4.0 is not a
# published installable package/version and must not be added to this file.

# ... existing runtime dependencies are retained unchanged ...
ollama==0.6.2
